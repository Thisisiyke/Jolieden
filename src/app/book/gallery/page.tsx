import PlaceholderShell from "@/components/demo/PlaceholderShell";

export default function BookGalleryPage() {
  return (
    <PlaceholderShell
      surface="Client booking · /book/gallery"
      phase="Stub · gallery + filters land in P5"
      personaName="Anonymous browser"
      personaRole="No identification yet"
      hint="Browse-only gallery: filter by category (Braids, Color, Silk Press…), tap a look to see what's included, identify only at the checkout step. Photos seeded in P6 from @joliedensbeautybar IG + the Shopify site."
    />
  );
}
